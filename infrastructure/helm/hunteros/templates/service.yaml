apiVersion: v1
kind: Service
metadata:
  name: {{ include "hunteros.fullname" . }}-api
  labels:
    app: {{ include "hunteros.name" . }}-api
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "hunteros.name" . }}-api
